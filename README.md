# raptor-midi-usb-watcher

Tiny helper service that watches for USB MIDI controllers on ALSA sequencer, updates
`raptor-midi-io-service` configuration, and triggers configuration reload on the MIDI I/O service.

Status: scaffold (first pass).
